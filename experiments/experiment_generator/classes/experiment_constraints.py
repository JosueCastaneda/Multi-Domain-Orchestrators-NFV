class ExperimentConstraints:

    def __init__(self, delay, bandwidth, loss, jitter):
        self.delay = delay
        self.bandwidth = bandwidth
        self.loss = loss
        self.jitter = jitter
