class UpdateConfigurationByOss:

    def __init__(self, name_of_vnf_to_change, operation, first_operation, max_delay, local_search, received_clock, passed_time):
        self.name_of_vnf_to_change = name_of_vnf_to_change
        self.operation = operation,
        self.first_operation = first_operation
        self.max_delay = max_delay
        self.local_search = local_search
        self.received_clock = received_clock
        self.passed_time = passed_time
